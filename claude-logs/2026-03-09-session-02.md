# Session Log — 2026-03-09 (Session 02)

## Summary
Continued from session 01 (2026-03-08). Completed the full benchmark eval, built the navigable results database on the website, added KCL support for Zoo, and set up Modal for academic model inference.

---

## What We Did

### 1. Full Benchmark Run — 4 models × 20 prompts
Ran the complete benchmark after fixing a network connectivity issue from the previous session.

**Results:**

| Model | Overall | T1 | T2 | T3 | T4 | Avg Latency |
|-------|---------|----|----|----|----|-------------|
| Claude Opus 4.6 | 95% (19/20) | 5/5 | 5/5 | 5/5 | 4/5 | 6.9s |
| Zoo ML-ephant | 95% (19/20) | 5/5 | 5/5 | 5/5 | 4/5 | 11.1s |
| Gemini 2.5 Flash | 70% (14/20) | 5/5 | 4/5 | 4/5 | 1/5 | 3.1s |
| GPT-5 | 60% (12/20) | 5/5 | 4/5 | 3/5 | 0/5 | 16.1s |

**Key failure modes:**
- GPT-5: hit 2048 token limit on complex prompts → truncated code, extract_code() returns empty
- Claude: 1 OpenCASCADE geometry crash (countersunk hole, gp_Vec zero norm)
- Gemini: hallucinated `.gear()` and `.cutExtrude()` methods; also wrong variable names
- Zoo: 1 timeout on helical spring (>120s)

**Eval infrastructure files:**
- `eval/run.py` — main runner
- `eval/models.py` — API clients (fixed Gemini rate-limit retry)
- `eval/execute.py` — CadQuery subprocess executor
- Results: `eval/results/20260308_203415/` (LLMs) + `eval/results/20260308_zoo/` (Zoo)

---

### 2. Zoo KCL Code Capture
Discovered that Zoo's job status response includes a `code` field with KCL (KittyCAD Language) source. Updated `eval/models.py` to capture it. Re-ran Zoo eval → `eval/results/20260309_zoo_kcl/`.

19/20 Zoo prompts now have KCL source. Example:
```kcl
@settings(defaultLengthUnit = mm)
cube = startSketchOn(XY)
  |> rectangle(center = [0, 0], width = size, height = size)
  |> extrude(length = size)
```

---

### 3. Navigable Results Database on Website
Built `/results` and `/results/[model]/[prompt]` pages.

**`/results`:**
- Leaderboard overview with 4-model summary cards
- Full table: all 20 prompts × 4 models by tier
- Each cell: ✓/✗ + badges (3D, PY/KCL) — click to detail page

**`/results/[model]/[prompt]`:**
- Full prompt text
- Auto-rotating 3D STL viewer (Three.js, blueprint-blue mesh)
- Source code panel with language badge (CadQuery Python / KCL)
- Compare section: same prompt for all other models with mini 3D previews

**Files created:**
- `app/results/page.tsx`
- `app/results/[model]/[prompt]/page.tsx`
- `app/components/STLViewer.tsx` — Three.js STL viewer with auto-rotate
- `app/data/results.json` — 80 structured result entries
- `public/eval/` — 26MB STL meshes + Python/KCL source files

**Tech notes:**
- Three.js `STLLoader` with dynamic import (client component)
- `generateStaticParams()` → 97 static pages total
- Language tag per result: `output_type: "cadquery" | "kcl"`

---

### 4. Modal Setup for Academic Models
User authenticated Modal account (workspace: maksym-d-bondarenko).

**Written Modal runners:**
- `eval/modal_text2cadquery.py` — Text-to-CadQuery (ricemonster/qwen2.5-3B-SFT + Mistral-7B-lora)
- `eval/modal_flexcad.py` — FlexCAD (microsoft/FlexCAD, 8B LoRA on Llama-3)
- `eval/modal_text2cad.py` — Text2CAD scaffold (from previous session, HF slug unverified)

**Text-to-CadQuery test run (Qwen 3B):**
- 20/20 code generated at ~7-18s/prompt on A10G
- 15/20 (75%) execute to STL after executor fixes
- Failures: 3 token truncations (bumped to 1024), 1 OCC error, 1 truncated export line
- Known limitation: model uses normalized 0-1 units from DeepCAD training data (geometry correct, dimensions wrong)

**Executor fixes (`eval/execute.py`):**
- Mocked `cq.exporters.export()` as no-op (Text-to-CadQuery embeds hardcoded export calls)
- Added fallback variable name resolution: checks `assembly`, `part_1`, `plate`, `bracket`, `gear`, `bolt`, etc. when `result` is not found

---

### 5. Academic Model Availability Research

| Model | Weights | HF Slug | Status |
|-------|---------|---------|--------|
| Text-to-CadQuery | ✓ | `ricemonster/qwen2.5-3B-SFT` | Tested — 75% |
| FlexCAD | ✓ | `microsoft/FlexCAD` | Runner written, not yet tested |
| CAD-Coder | ✓ | `CADCODER/CAD-Coder` (13B) | Runner not yet written |
| Text2CAD | partial | `SadilKhan/Text2CAD` (dataset, model slug unverified) | Scaffold exists |
| CAD-GPT | ✗ | not released | Blocked until ~June 2025 |

---

## Pending / Next Steps

1. **Re-run Text-to-CadQuery** with `max_new_tokens=1024` to fix 3 truncation failures
2. **Integrate Text-to-CadQuery into website** — copy STLs to `public/eval/text2cadquery-qwen/`, add to `results.json`
3. **Test FlexCAD**: `modal run eval/modal_flexcad.py --prompt "A cube 20mm"` — verify OBJ→STL pipeline works
4. **Write CAD-Coder Modal runner** (13B LLaVA-based, needs dual env)
5. **Verify Text2CAD HF slug** and test modal_text2cad.py
6. **Action item (saved from prev session)**: Clickable model page with live text prompt box + 3D CAD viewer

---

## Key Files Changed This Session

```
eval/models.py              — Zoo KCL capture, output_type="kcl"
eval/execute.py             — no-op export mock, fallback variable names
eval/modal_text2cadquery.py — NEW: Text-to-CadQuery Modal runner
eval/modal_flexcad.py       — NEW: FlexCAD Modal runner
eval/results/20260308_203415/  — Full LLM benchmark results
eval/results/20260308_zoo/     — Zoo benchmark results
eval/results/20260309_zoo_kcl/ — Zoo re-run with KCL code
app/results/page.tsx           — NEW: results leaderboard page
app/results/[model]/[prompt]/page.tsx — NEW: result detail page
app/components/STLViewer.tsx   — NEW: Three.js STL viewer
app/data/results.json          — NEW: structured benchmark data
public/eval/                   — NEW: STL meshes + source code files
```

---

## Commit History This Session

```
bf9483a  add Text-to-CadQuery + FlexCAD Modal runners; fix executor for academic models
377aa7e  add KCL source code for Zoo results + language labels in UI
5e94db6  add navigable results database with 3D STL viewer
402ebbd  run full benchmark: 4 models × 20 prompts, first complete eval results
```
