Order of Attack — CAD Arena
Date: 2026-03-22 (updated)

Goal: Get cadarena.dev publishable as a research arena.

─────────────────────────────────────────────────────────────────────
PHASE 1 — Clean Up the Static Site                          ✓ DONE
─────────────────────────────────────────────────────────────────────

[x] 1a. Content audit on landing page
[x] 1b. Reframe static results clearly

─────────────────────────────────────────────────────────────────────
PHASE 2 — Methodology Page                                  ✓ DONE
─────────────────────────────────────────────────────────────────────

[x] 2a. Create /methods page

─────────────────────────────────────────────────────────────────────
PHASE 3 — Dynamic Generation ("Try It")                     IN PROGRESS
─────────────────────────────────────────────────────────────────────

[x] 3a. /api/generate API route (Claude, GPT, Gemini, Zoo — SSE streaming)
[x] 3b. /try page frontend (prompt input, model selector, loading states, code display)
        NOTE: Zoo shows 3D viewer (STL via base64). LLMs show code only — no viewer yet.

[ ] 3c. CadQuery execution via Modal
        - POST prompt to Modal → run CadQuery → return STL as base64
        - Wire into /api/generate for Claude/GPT/Gemini routes
        - Reuse eval/modal_text2cadquery.py as reference for the execution env
        - Return stlBase64 same as Zoo does today

[ ] 3d. Upstash Redis rate limiting (replaces Supabase for this)
        - Vercel Edge middleware
        - IP-based: 5 prompts/day anonymous
        - No accounts needed

─────────────────────────────────────────────────────────────────────
PHASE 4 — Ship                                              NEXT
─────────────────────────────────────────────────────────────────────

[ ] 4a. Set env vars on Vercel (ANTHROPIC_API_KEY, OPENAI_API_KEY,
        GOOGLE_API_KEY, ZOO_API_TOKEN, UPSTASH_REDIS_REST_URL,
        UPSTASH_REDIS_REST_TOKEN, MODAL_API_KEY or webhook URL)

[ ] 4b. Deploy to cadarena.dev, smoke test /try end-to-end

─────────────────────────────────────────────────────────────────────
DEFERRED (see cadarena/FUTURE_FEATURES.md)
─────────────────────────────────────────────────────────────────────

- /explore page (needs Supabase DB + storage + consent UI — deferred until traffic)
- Auth + per-user quotas (deferred until /explore matters)
- Geometric accuracy metrics (Chamfer distance, VLM scoring)

─────────────────────────────────────────────────────────────────────
NOTES
─────────────────────────────────────────────────────────────────────
- Zoo returns STL as base64 in the API response — no storage needed
- CadQuery execution: Modal endpoint returns STL as base64 — same pattern
- No Supabase needed for v1 ship
- Rate limiting via Upstash Redis (free tier, no user accounts)
